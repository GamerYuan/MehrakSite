# Implementation Plans

Generated by the improve skill on 2026-06-23 at commit `3da69c5`. Execute in
the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row
when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Establish Vitest test baseline with characterization tests | P1 | M | — | DONE |
| 002  | Add OxLint + oxfmt + jsconfig + editorconfig | P2 | S | — | DONE |
| 003  | Add .env.example for VITE_APP_BACKEND_URL | P2 | S | — | DONE |
| 004  | Replace stale Vite template README with project README | P2 | S | 003 | DONE |
| 005  | Fix AGENTS.md drift — useGameView location and router mode | P3 | S | — | TODO |
| 006  | Fix docs permission check — remove lowercase mismatch | P1 | S | 001 (rec) | DONE |
| 007  | Fix transient /users/me failure permanently locking user out | P1 | S | 001 (rec) | DONE |
| 008  | Replace normalizeUser mass-spread with explicit allowlist | P2 | S | 007 | DONE |
| 009  | Add cancellation token to UserPortraitUploadModal onFileChange | P2 | S | — | TODO |
| 010  | Fix alias delete loop — refetch on partial failure | P2 | S | — | TODO |
| 011  | Deduplicate normalizeUser in UserManagementView | P2 | S | 008 | TODO |
| 012  | Centralize _redirected error handling with handleApiError helper | P2 | M | 001 (rec) | TODO |
| 013  | Add CSRF defense documentation and frontend coordination notes | P2 | M | 012 | TODO |
| 014  | Add CSP and browser hardening headers via public/_headers | P2 | S | — | TODO |
| 015  | Add route-level code splitting to reduce initial bundle size | P2 | S | — | TODO |
| 016  | Consolidate game registry — kill configMap, remove dead import | P2 | S | 001 (rec) | TODO |
| 017  | Spike — investigate Seaweed Filer integration vs demotion | P3 | S | — | TODO |

## Dependency notes

- **001 → 006, 007, 012, 016**: Test baseline (vitest) should land before
  bug fixes and refactors so they're verified by characterization tests.
  "rec" means recommended, not blocking — the plans work without tests but
  are riskier.
- **003 → 004**: README setup instructions reference `.env.example`.
- **007 → 008**: Both touch `useAuth.js`. Plan 007 is a one-line removal;
  Plan 008 modifies the `normalizeUser` function. Land 007 first so the
  diff is clean.
- **008 → 011**: Plan 011 makes `UserManagementView` import `normalizeUser`
  from `useAuth.js`. Plan 008 changes what `normalizeUser` returns (allowlist
  vs spread). Land 008 first so both call sites use the same version.
- **012 → 013**: Both touch `useApi.js`. Plan 012 adds `handleApiError` and
  refactors 41 call sites. Plan 013 adds CSRF token logic. Land 012 first so
  the CSRF changes don't conflict with the refactor.
- **014 + 017**: If the Seaweed Filer spike (017) recommends iframe
  integration, Plan 014's CSP must add `frame-src` for the backend domain.
  Execute 017 before finalizing 014 if integration is likely.

## Findings considered and rejected

- **#3 — UserManagementView has no UI control for isActive**: Not planned
  per user decision. The `isActive` field is silently frozen at whatever the
  server has. If deactivation is needed later, add a checkbox to both modals.
- **i1 — Seaweed Filer access gated client-side only**: User confirmed the
  backend has authorization gating against the owner Discord user ID. Not a
  finding — the client `v-if` is UX only, backend enforces.
- **i2 — Docs create has no per-game permission gate**: User confirmed
  backend authorization checks the owner Discord user ID. The client-side
  fix in Plan 006 (adding `hasGameWriteAccess` to `handleSave`) is for UX
  consistency, not security.
- **i3 — Object-addressable endpoints by ID (IDOR surface)**: User confirmed
  backend has authorization setup gating against the owner Discord user ID
  for all object-addressable endpoints. Not a finding.
- **D1 — Ship Tears of Themis**: Not planned. ToT has `routeKey: null` and
  no `gameConfigs` entry; the meta/permissions/DocCard branches are ghost
  plumbing. Ship or remove when backend ToT endpoints are ready.
- **D2 — Bulk character add**: Not needed for now. The API accepts arrays
  but the UI forces one-at-a-time. Low priority — implement when character
  roster updates become frequent.
- **D5 — "Test credentials" step in profile add/edit**: Not currently
  planned but will be soon. Bad HoYoLAB cookies surface only at command-run
  time. Requires a backend verify endpoint.
- **D6 — Bulk permission grants in UserManagement**: Not needed. Lower
  frequency than character/codes management; the single-user modal is
  sufficient for the current moderator count.

## Notes

- All plans are written against commit `3da69c5` (2026-06-23). Each plan
  includes a drift check command to verify the codebase hasn't changed
  since the plan was written.
- The repo has no test/lint/typecheck scripts — Plans 001 and 002 establish
  that infrastructure. Plans marked "001 (rec)" in dependencies are safer
  to execute after 001 lands but can proceed without it.
- All plans use conventional commit messages matching the repo's existing
  style (see `git log --oneline -10`).
